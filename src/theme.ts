import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fb6105',
      contrastText: '#fff',
    },
    secondary: {
      main: '#000000',
      contrastText: '#fff',
    },
  },
});

export default theme;
